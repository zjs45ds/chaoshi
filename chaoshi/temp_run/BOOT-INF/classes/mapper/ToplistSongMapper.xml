<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.chaoshi.mapper.ToplistSongMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="org.example.chaoshi.entity.ToplistSong">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="toplist_id" property="toplistId" jdbcType="BIGINT"/>
        <result column="song_id" property="songId" jdbcType="BIGINT"/>
        <result column="rank" property="rank" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, toplist_id, song_id, `rank`, created_at, updated_at, deleted
    </sql>

    <!-- 插入排行榜歌曲关联 -->
    <insert id="insertToplistSong" parameterType="org.example.chaoshi.entity.ToplistSong" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO toplist_songs (toplist_id, song_id, `rank`, created_at, updated_at)
        VALUES (#{toplistId}, #{songId}, #{rank}, NOW(), NOW())
    </insert>

    <!-- 批量插入排行榜歌曲关联 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO toplist_songs (toplist_id, song_id, `rank`, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.toplistId}, #{item.songId}, #{item.rank}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- 根据ID查询排行榜歌曲关联 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplist_songs
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 根据排行榜ID查询歌曲关联 -->
    <select id="selectByToplistId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplist_songs
        WHERE toplist_id = #{toplistId} AND deleted = 0
        ORDER BY `rank` ASC
    </select>

    <!-- 根据排行榜ID分页查询歌曲关联 -->
    <select id="selectByToplistIdPage" resultMap="BaseResultMap">
        SELECT id, toplist_id, song_id, `rank`, created_at, updated_at, deleted
        FROM toplist_songs
        WHERE toplist_id = #{toplistId} AND deleted = 0
        ORDER BY `rank` ASC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计排行榜歌曲数量 -->
    <select id="countByToplistId" parameterType="java.lang.Long" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM toplist_songs
        WHERE toplist_id = #{toplistId} AND deleted = 0
    </select>

    <!-- 更新排行榜歌曲关联 -->
    <update id="updateToplistSong" parameterType="org.example.chaoshi.entity.ToplistSong">
        UPDATE toplist_songs
        <set>
            <if test="rank != null">
                `rank` = #{rank},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除排行榜歌曲关联 -->
    <update id="deleteToplistSong" parameterType="java.lang.Long">
        UPDATE toplist_songs
        SET deleted = 1, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 根据排行榜ID删除所有歌曲关联 -->
    <update id="deleteByToplistId" parameterType="java.lang.Long">
        UPDATE toplist_songs
        SET deleted = 1, updated_at = NOW()
        WHERE toplist_id = #{toplistId} AND deleted = 0
    </update>

    <!-- 批量逻辑删除排行榜歌曲关联 -->
    <update id="deleteBatch">
        UPDATE toplist_songs
        SET deleted = 1, updated_at = NOW()
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

</mapper>
