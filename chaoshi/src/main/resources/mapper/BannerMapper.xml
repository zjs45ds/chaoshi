<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.chaoshi.mapper.BannerMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="org.example.chaoshi.entity.Banner">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="img_url" property="imgUrl" jdbcType="VARCHAR"/>
        <result column="link" property="link" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="enabled" property="enabled" jdbcType="BOOLEAN"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, title, img_url, link, sort, enabled, created_at, updated_at, deleted
    </sql>

    <!-- 查询所有启用的轮播图 -->
    <select id="selectEnabledBanners" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM banners
        WHERE deleted = 0 AND enabled = true
        ORDER BY sort ASC, created_at DESC
    </select>

    <!-- 根据ID查询轮播图 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM banners
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 插入轮播图 -->
    <insert id="insertBanner" parameterType="org.example.chaoshi.entity.Banner" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banners (title, img_url, link, sort, enabled, created_at, updated_at)
        VALUES (#{title}, #{imgUrl}, #{link}, #{sort}, #{enabled}, NOW(), NOW())
    </insert>

    <!-- 更新轮播图 -->
    <update id="updateBanner" parameterType="org.example.chaoshi.entity.Banner">
        UPDATE banners
        SET title = #{title},
            img_url = #{imgUrl},
            link = #{link},
            sort = #{sort},
            enabled = #{enabled},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除轮播图 -->
    <update id="deleteBanner" parameterType="java.lang.Long">
        UPDATE banners
        SET deleted = 1,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>