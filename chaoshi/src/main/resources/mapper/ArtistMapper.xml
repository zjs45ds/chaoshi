<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.chaoshi.mapper.ArtistMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="org.example.chaoshi.entity.Artist">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="LONGVARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, name, avatar, description, created_at, updated_at, deleted
    </sql>

    <!-- 插入歌手 -->
    <insert id="insertArtist" parameterType="org.example.chaoshi.entity.Artist" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO artists (name, avatar, description, created_at, updated_at)
        VALUES (#{name}, #{avatar}, #{description}, NOW(), NOW())
    </insert>

    <!-- 批量插入歌手 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO artists (name, avatar, description, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.avatar}, #{item.description}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- 根据ID查询歌手 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM artists
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 根据名称查询歌手 -->
    <select id="selectByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM artists
        WHERE name = #{name} AND deleted = 0
    </select>

    <!-- 分页查询歌手 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM artists
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计歌手数量 -->
    <select id="countArtists" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM artists
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
    </select>

    <!-- 更新歌手信息 -->
    <update id="updateArtist" parameterType="org.example.chaoshi.entity.Artist">
        UPDATE artists
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除歌手 -->
    <update id="deleteArtist" parameterType="java.lang.Long">
        UPDATE artists
        SET deleted = 1, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 批量逻辑删除歌手 -->
    <update id="deleteBatch">
        UPDATE artists
        SET deleted = 1, updated_at = NOW()
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

    <!-- 搜索歌手 -->
    <select id="searchArtists" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM artists
        WHERE deleted = 0
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY
        CASE
            WHEN name LIKE CONCAT(#{keyword}, '%') THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            ELSE 3
        END,
        created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 获取热门歌手 -->
    <select id="selectPopularArtists" resultMap="BaseResultMap">
        SELECT a.*
        FROM artists a
        LEFT JOIN songs s ON a.id = s.artist_id AND s.deleted = 0
        WHERE a.deleted = 0
        GROUP BY a.id
        ORDER BY COUNT(s.id) DESC
        LIMIT #{limit}
    </select>

</mapper>