<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaoshi.mapper.ToplistMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="com.chaoshi.entity.Toplist">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="cover" property="cover" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="LONGVARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, name, cover, description, created_at, updated_at, deleted
    </sql>

    <!-- 插入排行榜 -->
    <insert id="insertToplist" parameterType="com.chaoshi.entity.Toplist" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO toplists (name, cover, description, created_at, updated_at)
        VALUES (#{name}, #{cover}, #{description}, NOW(), NOW())
    </insert>

    <!-- 批量插入排行榜 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO toplists (name, cover, description, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.cover}, #{item.description}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- 根据ID查询排行榜 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 查询所有排行榜 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 分页查询排行榜 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计排行榜数量 -->
    <select id="countToplists" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM toplists
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
    </select>

    <!-- 更新排行榜信息 -->
    <update id="updateToplist" parameterType="com.chaoshi.entity.Toplist">
        UPDATE toplists
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="cover != null">
                cover = #{cover},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除排行榜 -->
    <update id="deleteToplist" parameterType="java.lang.Long">
        UPDATE toplists
        SET deleted = 1, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 批量逻辑删除排行榜 -->
    <update id="deleteBatch">
        UPDATE toplists
        SET deleted = 1, updated_at = NOW()
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

    <!-- 搜索排行榜 -->
    <select id="searchToplists" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE deleted = 0
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY
        CASE
            WHEN name LIKE CONCAT(#{keyword}, '%') THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            ELSE 3
        END,
        created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 获取热门排行榜 -->
    <select id="selectHotToplists" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE deleted = 0
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 获取最新排行榜 -->
    <select id="selectLatestToplists" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM toplists
        WHERE deleted = 0
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>