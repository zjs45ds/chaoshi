<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaoshi.mapper.UserFavoriteMvMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="com.chaoshi.entity.UserFavoriteMv">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="mv_id" property="mvId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, user_id, mv_id, created_at, deleted
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_favorite_mvs
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_favorite_mvs
        WHERE deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户ID查询收藏MV -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_favorite_mvs
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 检查用户是否收藏了指定MV -->
    <select id="existsByUserIdAndMvId" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM user_favorite_mvs
        WHERE user_id = #{userId} AND mv_id = #{mvId} AND deleted = 0
    </select>

    <!-- 查找已删除的收藏记录 -->
    <select id="findDeletedByUserIdAndMvId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_favorite_mvs
        WHERE user_id = #{userId} AND mv_id = #{mvId} AND deleted = 1
        LIMIT 1
    </select>

    <!-- 恢复已删除的收藏记录 -->
    <update id="restoreByUserIdAndMvId">
        UPDATE user_favorite_mvs
        SET deleted = 0, created_at = #{createdAt}
        WHERE user_id = #{userId} AND mv_id = #{mvId} AND deleted = 1
    </update>

    <!-- 插入收藏记录 -->
    <insert id="insert" parameterType="com.chaoshi.entity.UserFavoriteMv" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_favorite_mvs (user_id, mv_id, created_at)
        VALUES (#{userId}, #{mvId}, #{createdAt})
    </insert>

    <!-- 逻辑删除收藏记录 -->
    <update id="deleteByUserIdAndMvId">
        UPDATE user_favorite_mvs
        SET deleted = 1
        WHERE user_id = #{userId} AND mv_id = #{mvId} AND deleted = 0
    </update>

    <!-- 物理删除收藏记录 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user_favorite_mvs WHERE id = #{id}
    </delete>

    <!-- 分页查询用户收藏MV -->
    <select id="selectPageByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_favorite_mvs
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计用户收藏MV数量 -->
    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user_favorite_mvs
        WHERE user_id = #{userId} AND deleted = 0
    </select>

    <!-- 批量插入收藏记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO user_favorite_mvs (user_id, mv_id, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.mvId}, #{item.createdAt})
        </foreach>
    </insert>

    <!-- 批量删除收藏记录 -->
    <update id="batchDeleteByUserIdAndMvIds">
        UPDATE user_favorite_mvs
        SET deleted = 1
        WHERE user_id = #{userId} AND deleted = 0
        AND mv_id IN
        <foreach collection="mvIds" item="mvId" open="(" separator="," close=")">
            #{mvId}
        </foreach>
    </update>

</mapper>
